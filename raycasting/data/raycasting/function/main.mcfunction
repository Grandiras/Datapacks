# Main raycasting function - runs every tick
# Executes the start_ray function for each player in the game
# This initiates the raycasting process for all active players
execute as @a run function raycasting:start_ray